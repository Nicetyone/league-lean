// Stamped by install-league-lean.bat at install time. The default below ships
// with the repo for cases where the file wasn't regenerated (e.g. manual zip
// download). The installer overwrites this with the actual SHA + date.
export const VERSION = {
  sha: "dev",
  shortSha: "dev",
  date: "",
  branch: "main",
};
